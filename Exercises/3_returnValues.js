const calcArea = function(radius) {
    return area = 3.14 * radius ** 2;
    return area;
};

const area = calcArea(5);
console.log(area);

// Benefit? We can now use this value in another function e.g to calculate volume

const calcVol = function(area) {
    // code 
};

calcVol(area);